create database cpdtools;
use cpdtools;

create table tools (author varchar(255),
                    team varchar(255),
                    name varchar(255),
                    description mediumtext,
                    keywords varchar(255),
                    PRIMARY KEY (name)
);
